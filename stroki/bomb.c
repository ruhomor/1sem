#include <stdio.h>
#include <string.h>
#define	BUFF_SIZE 1024

char	*stripword()
{

}

int	main()
{
	char	str[BUFF_SIZE];

	while (NULL != fgets(str, BUFF_SIZE, stdin))
	{

	}
	printf("NO");
	return (0);
}
